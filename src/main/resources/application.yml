server:
  port: 8989

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_boot?useUnicode=true&characterEncoding=utf-8&useSSL=FALSE&serverTimezone=UTC
    username: root
    password: root
  jpa:
    show-sql: true

  thymeleaf:
    cache: false